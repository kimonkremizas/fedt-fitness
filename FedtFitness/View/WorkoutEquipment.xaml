<Page
    x:Class="FedtFitness.View.WorkoutEquipment"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FedtFitness.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:FedtFitness.ViewModel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Page.DataContext>
        <viewModel:FiltersViewModel/>
    </Page.DataContext>


    <Grid Margin="0,10,0,0">


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" Margin="496,-27,4,721" Grid.ColumnSpan="2">
            <TextBlock Text="Customize your workout" FontSize="30" Height="50"  HorizontalAlignment="Center" Margin="0,100,0,0"/>
            <StackPanel Orientation="Horizontal">
                <ComboBox Name="EquipmentCombobox"
                      HorizontalAlignment="Center"
                      PlaceholderText="Select equipment"
                      ItemsSource="{Binding AllEquipments, Mode = TwoWay  }" 
                      SelectedItem="{Binding SelectedEquipment, Mode=TwoWay}"
                      Width="200"
                      Margin="20,20,20,20">
                    <TextBlock Text="{Binding eqName}"/>

                </ComboBox> 

                <ComboBox Name="MuscleGroupCombobox"
                      HorizontalAlignment="Center"
                      PlaceholderText="Select muscle group"
                      ItemsSource="{Binding AllMuscleGroups, Mode=TwoWay }" 
                      DisplayMemberPath="{Binding mgName}" 
                      SelectedValuePath="ID"
                      SelectedItem="{Binding SelectedMuscleGroup, Mode=TwoWay}"
                      Width="200"
                      Margin="20,20,20,20"
                      />


            </StackPanel>

            <Button Content="Find Exercises" 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Click="Button_Click" />
        </StackPanel>
        <!-- List part -->
        <StackPanel VerticalAlignment="Top" Orientation="Vertical" Height="582" Grid.ColumnSpan="2" Margin="22,296,478,0" >
            <ListView ItemsSource="{Binding F1, Mode=TwoWay}" SelectedItem="{Binding Selectedexercise}" Height="100">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Margin="20,20,20,20" 
                                       VerticalAlignment="Center"
                                       FontSize="18"
                                       Text="{Binding EName}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>

        <!-- Details Part -->
        <StackPanel HorizontalAlignment="Right" Grid.Column="1" Margin="0,264,41,-19" Width="924" Grid.ColumnSpan="2">
            <StackPanel HorizontalAlignment ="Right"
                        Margin="10, 10, 10, 10"
                        BorderBrush="DarkOrange"
                        BorderThickness="3,3,3,3"
                        Orientation="Horizontal" Height="620" RenderTransformOrigin="0.5,0.5" Width="898">
                <StackPanel.RenderTransform>
                    <CompositeTransform ScaleX="-1"/>
                </StackPanel.RenderTransform>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="{}" RenderTransformOrigin="0.5,0.5">
                </TextBlock>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="{}"/>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}" Text="{}"/>
            </StackPanel>

        </StackPanel>


    </Grid>
</Page>
